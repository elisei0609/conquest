(function ($) {
   'use strict';
   
   $('.quality-images').slick({
       dots: false,
       speed: 300,
       slidesToShow: 1,
       slidesToScroll: 1,
       prevArrow: $('#arrow-left'),
       nextArrow: $('#arrow-right'),
       
      //  adaptiveHeight: true
     });
      
   })
(jQuery);